---
import { siteConfig } from '../../data/siteConfig';
import { NAVIGATION_LINKS } from '../../utils/constants';
import { getCurrentYear } from '../../utils/helpers';
import { Mail, Phone, MapPin } from 'lucide-react';

const currentYear = getCurrentYear();
---

<footer class="relative overflow-hidden footer-glass">
  <!-- Línea superior con gradiente neón -->
  <div class="absolute top-0 left-0 right-0 h-[2px] neon-border-top"></div>
  
  <!-- Background semitransparente con glass effect -->
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-gradient-to-br from-[#1e293b]/40 via-[#0f172a]/50 to-[#1e293b]/40"></div>
  </div>
  
  <!-- Grid pattern con neón -->
  <div class="absolute inset-0 opacity-[0.04] z-[1]">
    <div class="absolute inset-0 bg-grid-neon"></div>
  </div>
  
  <!-- Blobs de color animados -->
  <div class="absolute inset-0 z-0">
    <div class="absolute top-0 right-1/4 w-[500px] h-[500px] bg-[#3e6680]/10 rounded-full blur-[140px] animate-pulse-slow"></div>
    <div class="absolute bottom-0 left-1/3 w-[450px] h-[450px] bg-[#b4cfa6]/12 rounded-full blur-[130px] animate-pulse-slower"></div>
  </div>
  
  <!-- Main Footer Content -->
  <div class="container-custom px-4 md:px-8 py-12 sm:py-16 relative z-10">
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 sm:gap-12">
      
      <!-- Columna 1: Brand -->
      <div>
        <div class="mb-6">
          <div class="flex items-center space-x-4 mb-4">
            <!-- Logo con glow neón -->
            <div class="relative logo-container group cursor-pointer">
              <div class="absolute inset-0 w-36 sm:w-40 md:w-52 lg:w-60 h-36 sm:h-40 md:h-52 lg:h-60 -left-2 -top-2 bg-[#b4cfa6]/20 rounded-full blur-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 logo-glow"></div>
              
              <img 
                src="/images/logo/logo3d.svg" 
                alt="Vete a la Cima Logo"
                width="500"
                height="500"
                loading="lazy"
                class="relative w-32 sm:w-36 md:w-48 lg:w-56 h-32 sm:h-36 md:h-48 lg:h-56 object-contain transition-all duration-500 group-hover:scale-110 footer-logo-white"
              />
            </div>
            <div>
              <span class="font-heading font-bold text-lg sm:text-xl md:text-xl block leading-tight text-white text-glow">
                VETE A LA CIMA
              </span>
              <span class="text-[#b4cfa6] text-xs sm:text-xs font-medium block mt-1 neon-text">
                Estrategia, digitalización y crecimiento para el sector veterinario
              </span>
            </div>
          </div>
        </div>
        <p class="text-white/80 text-sm sm:text-sm leading-relaxed mb-4 sm:mb-6">
          Consultoría estratégica especializada en negocios veterinarios y empresas del sector pet.
          Acompaño a clínicas, profesionales y organizaciones a tomar mejores decisiones, lanzar nuevos proyectos y construir modelos de crecimiento sostenibles.
        </p>
        
        <!-- Social Links con efecto neón -->
        <div class="flex space-x-2 sm:space-x-3">
          <a 
            href="https://www.instagram.com/vir.veterinaria/" 
            target="_blank" 
            rel="noopener noreferrer"
            class="social-btn instagram-btn"
            aria-label="Instagram"
          >
            <svg class="w-4 sm:w-5 h-4 sm:h-5 text-white group-hover:scale-110 transition-transform relative z-10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
              <rect width="20" height="20" x="2" y="2" rx="5" ry="5"></rect>
              <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
              <line x1="17.5" x2="17.51" y1="6.5" y2="6.5"></line>
            </svg>
          </a>
          <a 
            href="https://www.linkedin.com/in/virginiavallejo?utm_source=share&utm_campaign=share_via&utm_content=profile&utm_medium=android_app" 
            target="_blank" 
            rel="noopener noreferrer"
            class="social-btn linkedin-btn"
            aria-label="LinkedIn"
          >
            <svg class="w-5 h-5 text-white group-hover:scale-110 transition-transform relative z-10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
              <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path>
              <rect width="4" height="12" x="2" y="9"></rect>
              <circle cx="4" cy="4" r="2"></circle>
            </svg>
          </a>
          <a 
            href={siteConfig.social.facebook} 
            target="_blank" 
            rel="noopener noreferrer"
            class="social-btn facebook-btn"
            aria-label="Facebook"
          >
            <svg class="w-5 h-5 text-white group-hover:scale-110 transition-transform relative z-10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
              <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path>
            </svg>
          </a>
          <a 
            href={`https://wa.me/${siteConfig.whatsapp}`}
            target="_blank" 
            rel="noopener noreferrer"
            class="social-btn whatsapp-btn"
            aria-label="WhatsApp"
          >
            <svg class="w-5 h-5 text-white group-hover:scale-110 transition-transform relative z-10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
              <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path>
            </svg>
          </a>
        </div>
      </div>

      <!-- Columna 2: Links Rápidos -->
      <div>
        <h3 class="footer-heading">
          Links Rápidos
          <span class="heading-underline"></span>
        </h3>
        <ul class="space-y-3">
          {NAVIGATION_LINKS.map((link) => (
            <li>
              <a 
                href={link.href}
                class="footer-link"
              >
                <span class="link-bar"></span>
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Columna 3: Servicios -->
      <div class="hidden lg:block">
        <h3 class="footer-heading">
          Servicios
          <span class="heading-underline"></span>
        </h3>
        <ul class="space-y-3">
          <li>
            <a href="/servicios#starter" class="footer-link">
              <span class="link-bar"></span>
              Plan Starter
            </a>
          </li>
          <li>
            <a href="/servicios#growth" class="footer-link">
              <span class="link-bar"></span>
              Plan Growth
            </a>
          </li>
          <li>
            <a href="/servicios#elite" class="footer-link">
              <span class="link-bar"></span>
              Plan Elite
            </a>
          </li>
          <li>
            <a href="/contacto" class="footer-link">
              <span class="link-bar"></span>
              Diagnóstico Gratuito
            </a>
          </li>
        </ul>
      </div>

      <!-- Columna 4: Contacto -->
      <div>
        <h3 class="footer-heading">
          Contacto
          <span class="heading-underline"></span>
        </h3>
        <ul class="space-y-4">
          <li class="contact-item">
            <div class="contact-icon">
              <Mail size={16} stroke="white" class="!text-white relative z-10" />
            </div>
            <a 
              href={`mailto:${siteConfig.email}`}
              class="text-white/80 hover:text-[#b4cfa6] transition-colors duration-300 text-sm break-all"
            >
              {siteConfig.email}
            </a>
          </li>
          <li class="contact-item">
            <div class="contact-icon">
              <Phone size={16} stroke="white" class="!text-white relative z-10" />
            </div>
            <a 
              href={`tel:${siteConfig.phone}`}
              class="text-white/80 hover:text-[#b4cfa6] transition-colors duration-300 text-sm"
            >
              {siteConfig.phone}
            </a>
          </li>
          <li class="contact-item">
            <div class="contact-icon">
              <MapPin size={16} stroke="white" class="!text-white relative z-10" />
            </div>
            <span class="text-white/80 text-sm">
              Argentina & Latinoamérica
            </span>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Bottom Bar con glass effect -->
  <div class="border-t border-white/10 relative z-10 bottom-bar">
    <div class="container-custom px-4 md:px-8 py-6">
      <div class="flex flex-col md:flex-row items-center justify-between space-y-4 md:space-y-0">
        <p class="text-white/70 text-sm text-center md:text-left">
          © {currentYear} {siteConfig.name}. Todos los derechos reservados.
        </p>
        
        <!-- Developer Credit con efecto neón -->
        <a 
          href="https://devcraftpablo.online/" 
          target="_blank" 
          rel="noopener noreferrer"
          class="devcraft-badge"
        >
          <div class="absolute inset-0 devcraft-glow"></div>
          <div class="absolute inset-0 shine-wrapper">
            <div class="shine-effect"></div>
          </div>
          
          <span class="relative text-white/80 text-xs sm:text-sm font-medium z-10">Desarrollado por</span>
          <img 
            src="/images/logo/devcraft.webp" 
            alt="DevCraft Logo" 
            width="150"
            height="50"
            loading="lazy"
            class="relative h-5 sm:h-6 md:h-7 w-auto object-contain group-hover:scale-110 transition-transform duration-500 filter drop-shadow-[0_0_12px_rgba(147,51,234,0.6)] group-hover:drop-shadow-[0_0_20px_rgba(147,51,234,0.9)] z-10"
          />
          <span class="relative font-bold text-base sm:text-lg md:text-lg devcraft-text z-10">DevCraft</span>
        </a>
        
        <div class="flex space-x-6">
          <a href="/privacidad" class="text-white/70 hover:text-[#b4cfa6] hover:text-shadow-neon transition-all duration-300 text-sm">
            Privacidad
          </a>
          <a href="/terminos" class="text-white/70 hover:text-[#b4cfa6] hover:text-shadow-neon transition-all duration-300 text-sm">
            Términos
          </a>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Línea inferior con gradiente neón -->
  <div class="absolute bottom-0 left-0 right-0 h-[1px] neon-border-bottom"></div>
</footer>

<style>
  /* Footer glass morphism */
  .footer-glass {
    background: rgba(30, 41, 59, 0.6);
    backdrop-filter: blur(25px) saturate(180%);
    -webkit-backdrop-filter: blur(25px) saturate(180%);
    box-shadow: 
      0 -4px 30px rgba(0, 0, 0, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
  }

  /* Líneas neón */
  .neon-border-top {
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(180, 207, 166, 0.6) 20%,
      rgba(62, 102, 128, 0.8) 50%,
      rgba(180, 207, 166, 0.6) 80%,
      transparent 100%
    );
    box-shadow: 
      0 0 10px rgba(180, 207, 166, 0.5),
      0 0 20px rgba(62, 102, 128, 0.3);
    animation: neon-pulse 3s ease-in-out infinite;
  }

  .neon-border-bottom {
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(180, 207, 166, 0.4) 30%,
      rgba(62, 102, 128, 0.6) 50%,
      rgba(180, 207, 166, 0.4) 70%,
      transparent 100%
    );
    box-shadow: 
      0 0 8px rgba(180, 207, 166, 0.3),
      0 0 15px rgba(62, 102, 128, 0.2);
  }

  @keyframes neon-pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
  }

  /* Grid neón */
  .bg-grid-neon {
    background-image: 
      linear-gradient(rgba(180, 207, 166, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(180, 207, 166, 0.04) 1px, transparent 1px);
    background-size: 50px 50px;
  }

  /* Animaciones blobs */
  @keyframes pulse-slow {
    0%, 100% { transform: scale(1); opacity: 0.1; }
    50% { transform: scale(1.1); opacity: 0.15; }
  }

  @keyframes pulse-slower {
    0%, 100% { transform: scale(1); opacity: 0.12; }
    50% { transform: scale(1.15); opacity: 0.18; }
  }

  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }

  .animate-pulse-slower {
    animation: pulse-slower 12s ease-in-out infinite;
  }

  /* Texto con glow */
  .text-glow {
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
  }

  .neon-text {
    text-shadow: 
      0 0 10px rgba(180, 207, 166, 0.5),
      0 0 20px rgba(180, 207, 166, 0.3);
  }

  /* Logo glow */
  .logo-glow {
    animation: logo-glow-pulse 3s ease-in-out infinite;
  }

  @keyframes logo-glow-pulse {
    0%, 100% { 
      transform: scale(1); 
      opacity: 0; 
    }
    50% { 
      transform: scale(1.2); 
      opacity: 1; 
    }
  }

  /* Logo blanco del footer con efecto similar al header */
  .footer-logo-white {
    filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 12px rgba(180, 207, 166, 0.4)) !important;
    -webkit-filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 12px rgba(180, 207, 166, 0.4)) !important;
    animation: footer-logo-pulse 2.2s cubic-bezier(0.4,0,0.2,1) infinite alternate;
  }

  @keyframes footer-logo-pulse {
    0% {
      filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 10px #b4cfa6) drop-shadow(0 0 5px #3e6680) !important;
      -webkit-filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 10px #b4cfa6) drop-shadow(0 0 5px #3e6680) !important;
    }
    100% {
      filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 28px #b4cfa6) drop-shadow(0 0 14px #3e6680) !important;
      -webkit-filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 28px #b4cfa6) drop-shadow(0 0 14px #3e6680) !important;
    }
  }

  .logo-container:hover .footer-logo-white {
    filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 20px rgba(180, 207, 166, 0.7)) drop-shadow(0 0 30px rgba(62, 102, 128, 0.5)) !important;
    -webkit-filter: brightness(15) saturate(0) contrast(4) drop-shadow(0 0 20px rgba(180, 207, 166, 0.7)) drop-shadow(0 0 30px rgba(62, 102, 128, 0.5)) !important;
  }

  /* Social buttons con neón */
  .social-btn {
    position: relative;
    width: 40px;
    height: 40px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    overflow: hidden;
    cursor: pointer;
  }

  .social-btn::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 12px;
    padding: 2px;
    background: linear-gradient(45deg, transparent, transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .social-btn:hover {
    transform: translateY(-2px);
    border-color: rgba(180, 207, 166, 0.4);
  }

  .instagram-btn:hover::before {
    background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888);
    opacity: 1;
  }

  .instagram-btn:hover {
    box-shadow: 0 0 20px rgba(220, 39, 67, 0.4);
  }

  .linkedin-btn:hover::before {
    background: linear-gradient(45deg, #0077b5, #00a0dc);
    opacity: 1;
  }

  .linkedin-btn:hover {
    box-shadow: 0 0 20px rgba(0, 119, 181, 0.4);
  }

  .facebook-btn:hover::before {
    background: linear-gradient(45deg, #1877f2, #0866ff);
    opacity: 1;
  }

  .facebook-btn:hover {
    box-shadow: 0 0 20px rgba(24, 119, 242, 0.4);
  }

  .whatsapp-btn:hover::before {
    background: linear-gradient(45deg, #25d366, #128c7e);
    opacity: 1;
  }

  .whatsapp-btn:hover {
    box-shadow: 0 0 20px rgba(37, 211, 102, 0.4);
  }

  /* Footer headings */
  .footer-heading {
    font-weight: 600;
    font-size: 1.125rem;
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
    color: white;
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
  }

  .heading-underline {
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 48px;
    height: 2px;
    background: linear-gradient(90deg, #b4cfa6, transparent);
    box-shadow: 0 0 10px rgba(180, 207, 166, 0.5);
  }

  /* Footer links con efecto neón */
  .footer-link {
    color: rgba(255, 255, 255, 0.8);
    transition: all 0.3s ease;
    font-size: 0.875rem;
    display: inline-flex;
    align-items: center;
    position: relative;
  }

  .link-bar {
    width: 0;
    height: 2px;
    background: #b4cfa6;
    margin-right: 0;
    transition: all 0.3s ease;
    border-radius: 2px;
    box-shadow: 0 0 10px rgba(180, 207, 166, 0);
  }

  .footer-link:hover {
    color: #b4cfa6;
    text-shadow: 0 0 10px rgba(180, 207, 166, 0.6);
  }

  .footer-link:hover .link-bar {
    width: 8px;
    margin-right: 8px;
    box-shadow: 0 0 10px rgba(180, 207, 166, 0.8);
  }

  /* Contact items */
  .contact-item {
    display: flex;
    align-items: start;
    gap: 0.75rem;
  }

  .contact-icon {
    width: 32px;
    height: 32px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .contact-item:hover .contact-icon {
    background: rgba(180, 207, 166, 0.1);
    border-color: rgba(180, 207, 166, 0.3);
    box-shadow: 0 0 15px rgba(180, 207, 166, 0.3);
  }

  .contact-icon::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(180, 207, 166, 0.1);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .contact-item:hover .contact-icon::before {
    opacity: 1;
  }

  /* Bottom bar */
  .bottom-bar {
    background: rgba(10, 25, 41, 0.3);
    backdrop-filter: blur(10px);
  }

  /* DevCraft badge con neón */
  .devcraft-badge {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: rgba(88, 28, 135, 0.1);
    backdrop-filter: blur(15px);
    border-radius: 16px;
    border: 1px solid rgba(147, 51, 234, 0.2);
    transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
    overflow: hidden;
    cursor: pointer;
  }

  .devcraft-badge:hover {
    border-color: rgba(147, 51, 234, 0.4);
    transform: translateY(-2px);
    box-shadow: 0 0 30px rgba(147, 51, 234, 0.3);
  }

  .devcraft-glow {
    background: rgba(147, 51, 234, 0.15);
    border-radius: 16px;
    filter: blur(20px);
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  .devcraft-badge:hover .devcraft-glow {
    opacity: 1;
    animation: devcraft-pulse 2s ease-in-out infinite;
  }

  @keyframes devcraft-pulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 1; }
  }

  .shine-wrapper {
    overflow: hidden;
    border-radius: 16px;
  }

  .shine-effect {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.2),
      transparent
    );
    transition: left 0.6s ease;
  }

  .devcraft-badge:hover .shine-effect {
    left: 100%;
  }

  .devcraft-text {
    background: linear-gradient(90deg, #a78bfa, #818cf8, #6366f1);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Animación gradient removida para mejor rendimiento */

  .text-shadow-neon {
    text-shadow: 0 0 10px rgba(180, 207, 166, 0.6);
  }

  @media (max-width: 640px) {
    .social-btn {
      width: 36px;
      height: 36px;
    }
    
    .devcraft-badge {
      padding: 0.625rem 1rem;
      gap: 0.375rem;
    }
  }
</style>