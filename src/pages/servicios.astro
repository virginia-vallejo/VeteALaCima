---
import Layout from '../layouts/Layout.astro';
import SectionTitle from '../components/shared/SectionTitle.astro';
import PlanDetail from '../components/servicios/PlanDetail.astro';
import PlanComparison from '../components/servicios/PlanComparison.astro';
import FAQAccordion from '../components/react/FAQAccordion.jsx';
import Button from '../components/shared/Button.astro';
import { plans } from '../data/plans';
---

<Layout 
  title="Servicios - Vete a la Cima"
  description="Conocé nuestros planes de consultoría para veterinarias: Starter, Growth y Elite. Elegí el que mejor se adapte a tu etapa de crecimiento."
>
  <!-- Hero Section -->
  <section class="relative min-h-[85vh] flex items-center overflow-hidden pt-32 md:pt-40 pb-20 md:pb-28">
    <!-- Background image -->
    <div class="absolute inset-0">
      <img 
        src="/images/hero/carruselhero/3.webp" 
        alt="Servicios Background" 
        class="w-full h-full object-cover servicios-hero-image"
      />
    </div>
    
    <!-- Background enterprise oscuro con overlay más fuerte para mantener legibilidad -->
    <div class="absolute inset-0 bg-[#0a1929]/70"></div>
    
    <!-- Capa de profundidad con gradiente radial -->
    <div class="absolute inset-0 bg-gradient-radial from-[#1a2f42]/40 via-[#0a1929]/60 to-[#020914]/80"></div>
    
    <!-- Vignette -->
    <div class="absolute inset-0 bg-vignette"></div>
    
    <!-- Grid pattern -->
    <div class="absolute inset-0 bg-grid-pattern opacity-[0.03]"></div>
    
    <!-- Acentos de color -->
    <div class="absolute inset-0">
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-[#b4cfa6]/10 rounded-full blur-[120px] animate-float-slow"></div>
      <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-[#66D4FF]/8 rounded-full blur-[120px] animate-float-slow" style="animation-delay: 2s;"></div>
    </div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="max-w-4xl mx-auto text-center text-white animate-fade-in-up">
        <div class="inline-block px-6 py-2 bg-white/15 backdrop-blur-xl rounded-full mb-6 border border-white/20 shadow-2xl animate-float">
          <span class="text-[#b4cfa6] font-semibold text-sm uppercase tracking-wide">
            📊 Nuestros Servicios
          </span>
        </div>
        
        <h1 class="font-heading font-bold text-4xl md:text-5xl lg:text-6xl mb-6 bg-gradient-to-r from-white via-[#e3f9ca] to-white bg-clip-text text-transparent">
          Planes diseñados para cada etapa de crecimiento
        </h1>
        
        <p class="text-xl md:text-2xl text-white/80 mb-8 leading-relaxed">
          Desde veterinarias que empiezan hasta grupos consolidados que buscan expandirse
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <Button href="#comparacion" variant="secondary" size="lg" class="group relative overflow-hidden">
            <span class="relative z-10">Comparar planes</span>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-700"></div>
          </Button>
          <Button href="/contacto" variant="outline" size="lg" class="!text-white !border-white/60 hover:!bg-white/10 hover:!border-[#b4cfa6]/50 backdrop-blur-sm">
            Diagnóstico gratuito
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- Plan Details -->
  <section class="section-padding bg-[#0a1929] relative overflow-hidden">
    <!-- Acento decorativo -->
    <div class="absolute top-0 right-0 w-[500px] h-[500px] bg-[#b4cfa6]/5 rounded-full blur-[150px]"></div>
    <div class="absolute bottom-0 left-0 w-[500px] h-[500px] bg-[#66D4FF]/5 rounded-full blur-[150px]"></div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="space-y-16">
        {plans.map((plan) => (
          <PlanDetail plan={plan} />
        ))}
      </div>
    </div>
  </section>

  <!-- Comparison Table -->
  <section id="comparacion" class="section-padding bg-[#0a1929] scroll-mt-20 relative overflow-hidden">
    <!-- Acento decorativo -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-[#b4cfa6]/5 rounded-full blur-[150px]"></div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="text-center mb-12">
        <p class="text-sm font-semibold text-[#b4cfa6] uppercase tracking-wide mb-3">
          Todas las características
        </p>
        <h2 class="font-heading font-bold text-4xl md:text-5xl lg:text-6xl mb-6 text-white">
          Comparación de planes
        </h2>
        <p class="text-lg md:text-xl text-white/90 max-w-3xl mx-auto">
          Compará los tres planes lado a lado para tomar la mejor decisión
        </p>
      </div>

      <div class="mt-16">
        <PlanComparison />
      </div>
    </div>
  </section>

  <!-- Process Overview -->
  <section class="section-padding bg-[#0a1929] relative overflow-hidden">
    <!-- Grid pattern -->
    <div class="absolute inset-0 bg-grid-pattern opacity-[0.02]"></div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="text-center mb-12">
        <p class="text-sm font-semibold text-[#b4cfa6] uppercase tracking-wide mb-3">
          Proceso de onboarding
        </p>
        <h2 class="font-heading font-bold text-4xl md:text-5xl lg:text-6xl mb-6 text-white">
          ¿Cómo funciona?
        </h2>
        <p class="text-lg md:text-xl text-white/90 max-w-3xl mx-auto">
          Desde que elegís tu plan hasta que empezás a ver resultados
        </p>
      </div>

      <div class="mt-16 grid grid-cols-1 md:grid-cols-4 gap-8">
        <div class="group text-center bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 hover:border-[#b4cfa6]/50 transition-all duration-500 hover:scale-105 hover:shadow-[0_0_40px_rgba(180,207,166,0.2)]">
          <div class="w-20 h-20 mx-auto mb-6 rounded-2xl bg-gradient-to-br from-[#66D4FF] to-[#4A738A] text-white flex items-center justify-center text-3xl font-heading font-bold shadow-lg group-hover:shadow-[0_0_30px_rgba(102,212,255,0.5)] transition-shadow duration-300">
            1
          </div>
          <h3 class="font-heading font-semibold text-xl text-white mb-3 group-hover:text-[#e3f9ca] transition-colors">
            Elegís tu plan
          </h3>
          <p class="text-white/70">
            Seleccionás el plan que mejor se adapte a tus necesidades actuales
          </p>
        </div>

        <div class="group text-center bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 hover:border-[#b4cfa6]/50 transition-all duration-500 hover:scale-105 hover:shadow-[0_0_40px_rgba(180,207,166,0.2)]">
          <div class="w-20 h-20 mx-auto mb-6 rounded-2xl bg-gradient-to-br from-[#b4cfa6] to-[#8fa886] text-[#0a1929] flex items-center justify-center text-3xl font-heading font-bold shadow-lg group-hover:shadow-[0_0_30px_rgba(180,207,166,0.5)] transition-shadow duration-300">
            2
          </div>
          <h3 class="font-heading font-semibold text-xl text-white mb-3 group-hover:text-[#e3f9ca] transition-colors">
            Diagnóstico inicial
          </h3>
          <p class="text-white/70">
            Hacemos un análisis completo de tu veterinaria en una sesión de 90 minutos
          </p>
        </div>

        <div class="group text-center bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 hover:border-[#b4cfa6]/50 transition-all duration-500 hover:scale-105 hover:shadow-[0_0_40px_rgba(180,207,166,0.2)]">
          <div class="w-20 h-20 mx-auto mb-6 rounded-2xl bg-gradient-to-br from-[#66D4FF] to-[#4A738A] text-white flex items-center justify-center text-3xl font-heading font-bold shadow-lg group-hover:shadow-[0_0_30px_rgba(102,212,255,0.5)] transition-shadow duration-300">
            3
          </div>
          <h3 class="font-heading font-semibold text-xl text-white mb-3 group-hover:text-[#e3f9ca] transition-colors">
            Plan personalizado
          </h3>
          <p class="text-white/70">
            Diseñamos tu estrategia específica con objetivos, acciones y cronograma
          </p>
        </div>

        <div class="group text-center bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 hover:border-[#b4cfa6]/50 transition-all duration-500 hover:scale-105 hover:shadow-[0_0_40px_rgba(180,207,166,0.2)]">
          <div class="w-20 h-20 mx-auto mb-6 rounded-2xl bg-gradient-to-br from-[#b4cfa6] to-[#8fa886] text-[#0a1929] flex items-center justify-center text-3xl font-heading font-bold shadow-lg group-hover:shadow-[0_0_30px_rgba(180,207,166,0.5)] transition-shadow duration-300">
            4
          </div>
          <h3 class="font-heading font-semibold text-xl text-white mb-3 group-hover:text-[#e3f9ca] transition-colors">
            Implementación
          </h3>
          <p class="text-white/70">
            Empezamos a trabajar juntos en la ejecución y seguimiento continuo
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="section-padding bg-[#0a1929] relative overflow-hidden">
    <!-- Acento decorativo -->
    <div class="absolute top-1/2 right-0 w-96 h-96 bg-[#b4cfa6]/5 rounded-full blur-[120px]"></div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="text-center mb-12">
        <p class="text-sm font-semibold text-[#b4cfa6] uppercase tracking-wide mb-3">
          Sobre los servicios
        </p>
        <h2 class="font-heading font-bold text-4xl md:text-5xl lg:text-6xl mb-6 text-white">
          Preguntas frecuentes
        </h2>
        <p class="text-lg md:text-xl text-white/90 max-w-3xl mx-auto">
          Respondemos las dudas más comunes sobre nuestros planes
        </p>
      </div>

      <div class="mt-16">
        <FAQAccordion client:visible />
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="section-padding bg-[#0a1929] relative overflow-hidden">
    <!-- Background effects -->
    <div class="absolute inset-0 bg-gradient-radial from-[#1a2f42]/40 via-[#0a1929] to-[#020914]"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-[#b4cfa6]/10 rounded-full blur-[150px] animate-pulse-slow"></div>
    
    <div class="container-custom px-4 md:px-8 relative z-10">
      <div class="max-w-3xl mx-auto text-center text-white">
        <h2 class="font-heading font-bold text-3xl md:text-4xl lg:text-5xl mb-6 bg-gradient-to-r from-white via-[#e3f9ca] to-white bg-clip-text text-transparent">
          ¿Todavía no estás seguro cuál plan elegir?
        </h2>
        <p class="text-xl text-white/80 mb-10">
          Agenda una llamada de diagnóstico gratuita y te ayudamos a encontrar la mejor opción para tu veterinaria
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <Button href="/contacto" variant="secondary" size="lg" class="group relative overflow-hidden">
            <span class="relative z-10">Agendar diagnóstico gratuito</span>
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-700"></div>
          </Button>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Gradiente radial para profundidad */
  .bg-gradient-radial {
    background: radial-gradient(ellipse at center, var(--tw-gradient-stops));
  }

  /* Vignette efecto */
  .bg-vignette {
    background: radial-gradient(
      ellipse at center,
      transparent 0%,
      transparent 40%,
      rgba(0, 0, 0, 0.4) 100%
    );
  }

  /* Patrón de grid tecnológico */
  .bg-grid-pattern {
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 50px 50px;
  }

  /* Animaciones de entrada */
  @keyframes fade-in-up {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s cubic-bezier(0.23, 1, 0.32, 1) 0.2s both;
  }

  /* Float animation */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: float 6s ease-in-out infinite;
  }

  /* Pulse lento */
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.6;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.05);
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }

  /* Ajuste de posición de imagen hero */
  .servicios-hero-image {
    object-position: center -30%;
  }

  /* Ajuste responsive para mobile - imagen completamente responsive */
  @media (max-width: 768px) {
    .servicios-hero-image {
      object-position: center center;
      object-fit: cover;
      width: 100%;
      height: 100%;
    }
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }
</style>
